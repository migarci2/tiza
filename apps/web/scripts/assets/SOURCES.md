# Recording cursor assets

`macos-cursor.svg` and `macos-text-cursor.svg` are reused from the local Yuvo project, `videos/landing-demo/assets/`, at the user’s request. Used only in the browser-recording overlay; they are not application illustrations. Their original hotspot and geometry are preserved. The black arrow has a white outline and changes to the text insertion pointer over inputs.
